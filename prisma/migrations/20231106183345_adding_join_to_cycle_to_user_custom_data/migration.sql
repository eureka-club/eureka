BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[UserCustomData] ADD [joinToCycle] INT NOT NULL CONSTRAINT [UserCustomData_joinToCycle_df] DEFAULT -1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
