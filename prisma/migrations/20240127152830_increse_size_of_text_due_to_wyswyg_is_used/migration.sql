BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[backOfficeSettingsSliders] ALTER COLUMN [text] VARCHAR(4000) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
