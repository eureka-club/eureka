BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[works] ADD [ToCheck] BIT CONSTRAINT [works_ToCheck_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
