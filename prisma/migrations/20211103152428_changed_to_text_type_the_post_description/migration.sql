BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[posts] ALTER COLUMN [content_text] TEXT NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN 
    ROLLBACK TRAN;
END;
THROW

END CATCH
