BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Notification] DROP CONSTRAINT [Notification_fromUserId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
