BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[backOfficeSettings] ADD [FeaturedWorks] VARCHAR(60);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
