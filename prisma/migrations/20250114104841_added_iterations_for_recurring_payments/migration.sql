BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[cycles] ADD [iterations] INT NOT NULL CONSTRAINT [cycles_iterations_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
