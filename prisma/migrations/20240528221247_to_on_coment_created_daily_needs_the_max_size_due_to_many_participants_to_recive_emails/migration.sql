BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ComentCreatedDaily] ALTER COLUMN [to] VARCHAR(max) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
