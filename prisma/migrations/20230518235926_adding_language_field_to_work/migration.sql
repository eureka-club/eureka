BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[works] ADD [language] NVARCHAR(1000) NOT NULL CONSTRAINT [works_language_df] DEFAULT 'spanish';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
