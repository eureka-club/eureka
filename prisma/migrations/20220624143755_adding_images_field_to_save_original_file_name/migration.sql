BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[backOfficeSettings] ADD [SlideImage1] VARCHAR(200),
[SlideImage2] VARCHAR(200),
[SlideImage3] VARCHAR(200);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
