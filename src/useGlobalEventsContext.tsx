/* import { createContext, useContext} from 'react';

export type ContextType = {
  notify: CustomEvent;
};
export const GlobalEventsContext = createContext<ContextType>({
  notify: new CustomEvent('notify',{detail:null})
});

export const useGlobalEventsContext = (): ContextType => useContext(GlobalEventsContext);

 */
export {}